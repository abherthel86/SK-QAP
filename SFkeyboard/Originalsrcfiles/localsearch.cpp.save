#include "localsearch.h"
#define KEYS 140

void swap (Gendata *data, Keyboard *keyb){

	vector <int> aux_grid;

	for (int i = 0; i < KEYS; i++)
		aux_grid.push_back(keyb->grid[i]);


















}
